#include <iostream>
#include <vector>
#include <algorithm>
#define FOR(i,a,b) for(int i = a; i <= b; ++i)
using namespace std;

int main() {
    int n, x;
    vi h(n), s(n);
    FOR(i,0,n-1) cin >> h[i];
    FOR(i,0,n-1) cin >> s[i];
    
}